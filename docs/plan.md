# План проекта: kopeika

## Зафиксированные решения пользователя

| Вопрос | Ответ |
|--------|-------|
| Устройства | 1 сейчас, без облачного синка в v1; есть VM для backend |
| LLM API | можно использовать, если экономно |
| Основной банк | **Т-Банк** — формат CSV-выписки приоритет №1 |
| UI-стек | **TailwindCSS** |
| Монетизация | не делаем |
| Пользователи | 1 (одиночный режим), семейные аккаунты — v3+ |
| Валюты | мультивалютное с v1 |
| Платформа | основной пользователь — **iOS Safari**, тестируем в первую очередь там |
| Дизайн | `Копейка.html` — отправная точка; следующий шаг — перенос в Figma |

## Цель проекта

Создать iOS-first PWA для учёта личных финансов с мультимодальным вводом транзакций (текст, голос, импорт файлов), умной автокатегоризацией и мультивалютной поддержкой с v1. Приложение работает offline-first на клиенте (IndexedDB), обращается к backend только за обогащением (голос, LLM, бэкапы). Не привязано к банковским API, устанавливается как PWA без сторов.

## MVP scope (v1)

Минимальный продукт, которым можно реально пользоваться:

1. **Быстрый ручной ввод транзакций** — 2-3 тапа (сумма + категория). Основной способ ввода, должен быть быстрее всех конкурентов.
2. **Текстовый ввод в свободной форме** — «500₽ кофе», «обед 350», «такси 200₽ домой». Парсинг суммы, описания, попытка определить категорию по ключевым словам.
3. **Категории** — ~15-20 предустановленных (еда, транспорт, развлечения, одежда, здоровье, ЖКХ...) + CRUD для кастомных категорий.
4. **Автокатегоризация по правилам** — маппинг ключевых слов → категории («пятёрочка» → Продукты, «uber» → Транспорт). Обучение на корректировках пользователя.
5. **Круговая диаграмма расходов** — donut chart на главном экране с разбивкой по категориям. Фильтр по периоду (день/неделя/месяц/год).
6. **Импорт CSV** — первый парсер под формат Т-Банка + универсальный импорт с маппингом колонок.
7. **PWA с офлайн-режимом** — service worker, всё работает без интернета. Оптимизация под iOS Safari.
8. **Экспорт/импорт бэкапов** — JSON-файл для защиты от потери данных. Автоматический экспорт раз в N дней + ручной экспорт. Опциональный бэкап на сервер.
9. **Базовая статистика** — итоги за период, топ категорий, средний расход в день.
10. **Мультивалютность** — поддержка нескольких валют с v1 (хранение суммы + кода валюты, конвертация по курсу для отчётов).
11. **Backend** — минимальный сервер для LLM-прокси (категоризация сложных кейсов), Whisper API прокси (голос), приём бэкапов.

**Что НЕ входит в MVP:** сканирование чеков/OCR, бюджеты/лимиты, синхронизация между устройствами, PDF-импорт, общие кошельки, повторяющиеся транзакции.

## Этапы

| # | Этап | Описание | Объём |
|---|------|----------|-------|
| 1 | Каркас PWA | Vite + React + TypeScript + TailwindCSS + service worker + manifest.json + офлайн-shell. iOS-оптимизация: apple-touch-icon, splash, safe-area-insets | S |
| 2 | Backend: базовый сервер | Node 20 + Fastify + Pino + TypeScript. Эндпоинты: health, приём бэкапов. Деплой на VM пользователя | M |
| 3 | **Дизайн** | Анализ `Копейка.html`, перенос UI-kit в Figma (пользователь), живая страница компонентов в репо. Подробности — в разделе «Дизайн-фаза» ниже | M |
| 4 | Модель данных и хранилище | Схема транзакций (с полем валюты), категорий, настроек. IndexedDB через Dexie.js. Миграции | M |
| 5 | UI-kit и навигация | Базовые компоненты (кнопки, инпуты, модалки), роутинг, layout с нижней навигацией. **Итеративный режим: один экран = один PR** | M |
| 6 | Ручной ввод транзакций | Форма ввода: сумма, валюта, категория, дата, описание. Быстрый ввод за 2-3 тапа. Тач-таргеты ≥ 44pt, input type для чисел | M |
| 7 | Категории (CRUD) | Предустановленные категории + создание/редактирование/удаление кастомных с иконками | S |
| 8 | Текстовый ввод (NL) | Парсер «500₽ кофе» → {amount: 500, currency: "RUB", description: "кофе", category: "Кафе"}. Regex + словарь | M |
| 9 | Автокатегоризация (правила + LLM) | Маппинг ключевых слов → категории. Запоминание корректировок. LLM-fallback через backend для сложных кейсов | M |
| 10 | Главный экран и диаграммы | Dashboard: donut chart по категориям, итоги за период, список последних транзакций | M |
| 11 | Импорт CSV: Т-Банк | Парсер формата Т-Банка (приоритет №1). Загрузка файла, предпросмотр, маппинг категорий | M |
| 12 | Мультивалютность | Выбор валюты при вводе, хранение курсов, конвертация для отчётов, базовая валюта в настройках | M |
| 13 | Экспорт/импорт бэкапов | Экспорт всех данных в JSON, импорт с мержем/заменой. Авто-экспорт раз в N дней. Опциональная отправка на backend | S |
| 14 | PWA-оптимизация | Кеширование, install prompt, иконки, splash screen, тестирование offline-режима. Add to Home Screen инструкция | S |
| 15 | **--- v2 начинается здесь ---** | | |
| 16 | Голосовой ввод | Запись audio на клиенте → Whisper API на backend → текст → парсинг. Web Speech API не работает в standalone PWA на iOS | L |
| 17 | Бюджеты и лимиты | Установка лимитов по категориям, визуальный прогресс (как «светофор» CoinKeeper) | M |
| 18 | Тренды и аналитика | Сравнение периодов, линейные графики, топ трат, средние | M |
| 19 | Повторяющиеся транзакции | Автоматическое создание подписок, зарплаты, аренды | S |
| 20 | Шаблоны CSV банков РФ | Парсеры для Сбер, Альфа-Банк, ВТБ + автоопределение формата | M |
| 21 | Теги/метки | Кросс-категорийные теги (как у Toshl/Spendee) | S |
| 22 | **--- v3 начинается здесь ---** | | |
| 23 | Cloud-синхронизация | Backend для синка данных между устройствами | L |
| 24 | Сканирование чеков | Камера → OCR → транзакция (Web Camera API + LLM/OCR-сервис) | L |
| 25 | Общие кошельки | Семейный/парный бюджет с совместным доступом | L |
| 26 | Мониторинг подписок | Автоопределение повторяющихся списаний, напоминания | M |

**Размеры:** S = 1-3 дня, M = 3-7 дней, L = 1-2 недели

### Дизайн-фаза (этап 3 — подробности)

Отдельный этап между каркасом и началом разработки UI. Цель — зафиксировать визуальный язык приложения до написания кода.

**Шаг 1. Анализ `Копейка.html`**
Изучить стартовый набросок: какие экраны нарисованы, какие компоненты (карточки, графики, ввод, навигация), стиль, цвета, типографика. Составить полный список экранов и компонентов с вариантами состояний (empty / loading / error / filled).

**Шаг 2. Перенос в Figma (делает пользователь)**
Пользователь создаёт Figma-файл с UI-kit и экранами. Оркестратор готовит структуру:
- Список компонентов и их варианты
- Типографические токены (размеры, веса — из Inter Tight)
- Цветовые токены (из `Копейка.html`: фон #ECE7DD и производные)
- Набор иконок для категорий
- Все состояния каждого экрана

**Что ожидается от пользователя на этом этапе:** создать Figma-файл, перенести туда компоненты из HTML-скетча, утвердить цветовую палитру, типографику, стиль иконок. Прислать ссылку на Figma-файл.

**Шаг 3. Живая страница компонентов в репо**
Альтернатива/дополнение к Figma — `/uikit` route в самом приложении (или Storybook), где видны все компоненты в одном месте. Пользователь может посмотреть в браузере (в том числе на iPhone) и попросить правки.

**Шаг 4. Итеративная разработка UI**
Пользователь хочет детально контролировать каждый экран. На этапе кодинга UI каждый экран ревьюится отдельно, до перехода к следующему. **Режим: один экран = один PR.**

## Стек

### Frontend (v1+)

| Технология | Зачем |
|---|---|
| **TypeScript** | Типобезопасность, авто-документация через типы |
| **React 19** | UI-фреймворк, огромная экосистема |
| **Vite** | Быстрая сборка, PWA-плагин (vite-plugin-pwa) |
| **TailwindCSS** | Стилизация — решение пользователя |
| **Dexie.js** | Обёртка над IndexedDB — реактивные запросы, миграции, типизация |
| **Recharts** или **Chart.js** | Графики (pie chart, line chart, bar chart) |
| **Vitest** | Тесты (совместим с Vite) |
| **Workbox** (через vite-plugin-pwa) | Service worker, кеширование, офлайн |
| **date-fns** | Работа с датами (легче чем moment/dayjs) |

### Backend (v1+)

| Технология | Зачем |
|---|---|
| **Node 20** | Рантайм |
| **Fastify** | HTTP-фреймворк (быстрее Express, TypeScript-first) |
| **Pino** | Структурированное логирование |
| **TypeScript** | Общий язык с frontend |

Postgres только если появится server-side хранение данных. Для MVP хватит файлового хранилища для бэкапов на VM пользователя.

### Архитектура

**Принцип: клиент работает офлайн, идёт в backend только за обогащением.**

- **Клиент (PWA):** все данные в IndexedDB через Dexie.js. Полная функциональность офлайн: ввод, просмотр, статистика, экспорт.
- **Backend (VM пользователя):** четыре роли:
  1. **Whisper API прокси** — для голосового ввода (Web Speech API не работает в standalone PWA на iOS)
  2. **LLM прокси** — для категоризации сложных кейсов (экономный режим: только когда правила не справились)
  3. **Приёмник CSV-выписок** — опционально, если пользователь хочет загружать на сервер, а не парсить на клиенте
  4. **Бэкап-сервис** — export/import IndexedDB → файл на сервере, чтобы выжить 7-дневную чистку iOS
- **Нет синхронизации** в v1 — данные живут на клиенте, backend только сервисный.

### Формат CSV Т-Банка

Первый CSV-парсер пишем под формат Т-Банка (основной банк пользователя). Формат выписки Т-Банка: CSV с разделителем `;`, кодировка может быть cp1251 или UTF-8. Типичные колонки: дата операции, дата платежа, номер карты, статус, сумма операции, валюта операции, сумма платежа, валюта платежа, кэшбэк, категория, MCC, описание. Точная структура уточняется по реальному файлу выписки пользователя.

## iOS-приоритет

Основной пользователь — iOS Safari. Каждый этап разработки проверяется в Safari на iPhone (реальное устройство или responsive mode).

### PWA на iOS

- Корректный `manifest.json`: `display: standalone`, `theme-color`, `background-color`
- `apple-touch-icon` (180×180), splash screens для всех размеров iPhone
- `meta viewport` с `viewport-fit=cover` для работы с safe-area
- Add to Home Screen — обязательная инструкция в README приложения и onboarding-экране

### Ограничения iOS Safari

- **Web Speech API** не работает в standalone PWA → голосовой ввод только через Whisper API на backend (запись MediaRecorder → отправка audio → возврат текста)
- **IndexedDB** может быть очищен через 7 дней неиспользования в standalone PWA → обязательны явные бэкапы: авто-экспорт раз в N дней + ручной экспорт + опциональный бэкап на сервер
- **Push-уведомления** поддерживаются с iOS 16.4, но требуют пользовательского разрешения

### UI-требования для мобильного

- Тач-таргеты ≥ 44pt (44×44 CSS px)
- `input type="number"` / `inputmode="decimal"` для вызова числовой клавиатуры
- Корректная работа с `safe-area-inset-*` (вырез, home indicator)
- `position: fixed` — проверять на iOS (известные баги с виртуальной клавиатурой)
- Тестировать с включённым Dynamic Type (доступность)

## Риски и неопределённости

### Высокий риск

- **Голосовой ввод на iOS PWA** — Web Speech API не работает в standalone mode. Решение: backend с Whisper API (есть VM). Стоимость Whisper API: ~$0.006/мин.

- **Потеря данных на iOS** — IndexedDB может быть очищен через 7 дней неиспользования. Для финансового приложения это катастрофа. Митигация: авто-экспорт + бэкап на сервер (VM есть).

### Средний риск

- **Стоимость LLM API** — Claude Haiku ($1/$5 за MTok) — ~$0.001 за транзакцию. При 30 транзакциях в день = ~$1/мес. Митигация: LLM только как fallback, когда правила не справились (экономный режим).

- **Парсинг PDF-выписок** — каждый банк имеет уникальный формат PDF. Рекомендация: не делать в v1-v2.

- **Автокатегоризация без LLM** — правила по ключевым словам покрывают ~70-80% кейсов. Для оставшихся 20-30% — LLM через backend (решение принято).

- **Объём парсеров банковских CSV** — в РФ нет стандартного формата, каждый банк — своя структура, кодировка, формат дат. В v1 только Т-Банк, остальные в v2.

### Низкий риск

- **PWA-adoption** — пользователи не привыкли к PWA на телефоне. Нужен onboarding с инструкцией «Как установить». iOS 26 улучшает поддержку PWA.

- **Производительность с большим объёмом данных** — IndexedDB может тормозить при 10 000+ транзакций. Решается пагинацией и индексами в Dexie.js.

## Что НЕ делаем в v1

- Сканирование чеков / OCR
- Импорт PDF
- Синхронизация между устройствами
- Общие кошельки / семейный бюджет
- Бюджеты и лимиты (только трекинг)
- Повторяющиеся транзакции
- Push-уведомления
- Тёмная тема (можно добавить быстро, но не приоритет)
- Интеграция с банковскими API
- Мобильные виджеты
- Монетизация

## Что ещё уточнить

- **Бюджет на LLM в месяц** — какой потолок расходов на API? Нужен ли hard limit?
- **Какие конкретно валюты в MVP** — RUB + USD + EUR? Или настраиваемый список?
- **Частота авто-бэкапа** — раз в день? раз в 3 дня? При каждом изменении?
- **VM пользователя** — ОС, ресурсы, доступ? Есть ли Docker? Домен/IP для HTTPS?
- **Формат выписки Т-Банка** — нужен реальный файл CSV для точного парсера

## Источники дизайна

- **`Копейка.html`** (commit 311e3c49 в main, 2026-04-30) — стартовый набросок UI от пользователя через claude.ai artifacts. Содержимое: HTML-shell загружающий React 18 + три JSX-компонента (ios-frame, screens, app). Шрифт Inter Tight (400-700), фон #ECE7DD (тёплый бежевый). Экраны и компоненты определены в JSX-файлах (не включены в репозиторий, видны при живом просмотре).
- **Figma-файл** (TBD после этапа «Дизайн» — пользователь создаст и пришлёт ссылку).
